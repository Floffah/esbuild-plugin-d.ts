---
"esbuild-plugin-d.ts": major
---

chore: narrow the supported TypeScript peer range to 5.5 through current 5.x

This package uses the TypeScript compiler API directly and is now only supported
with `typescript >=5.5.0 <6`.
